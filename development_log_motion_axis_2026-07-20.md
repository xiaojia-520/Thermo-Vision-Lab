# Thermo-Vision-Lab 运动轴联调与开发日志

日期：2026-07-20  
范围：FMC4030 网络改址、三控制器通信验证、31 号控制器 X 轴运动/限位/回零测试、离线开发方案确认

## 1. 本次工作概览

本次主要完成以下工作：

1. 将三台 FMC4030 控制器从 `192.168.0.x` 网段迁移到实验舱所在的 `192.168.1.x` 网段；
2. 验证实验舱和三台控制器在同一网段上的网络与 SDK 通信；
3. 梳理 FMC4030 SDK 可用于本项目的功能范围；
4. 编写可输入参数的单轴安全测试脚本；
5. 对 `192.168.1.31` 的 X 轴进行停止、限位、回零、低速往返和限位重复性测试；
6. 确认控制器位置属于脉冲/指令坐标，不能直接视为真实机械反馈；
7. 确认当前适合采用软件零点，并明确无设备环境下的轴控制开发范围。

## 2. 网络配置与通信结果

### 2.1 IP 修改

三台 FMC4030 的 IP 参数写入结果：

| 原地址 | 新地址 | SDK 写入结果 |
|---|---|---|
| `192.168.0.31` | `192.168.1.31` | `Open=0, Get=0, Set=0, Close=0` |
| `192.168.0.32` | `192.168.1.32` | `Open=0, Get=0, Set=0, Close=0` |
| `192.168.0.33` | `192.168.1.33` | `Open=0, Get=0, Set=0, Close=0` |

控制器断电重启后，电脑物理网卡配置为：

```text
IP：192.168.1.1
掩码：255.255.255.0
```

### 2.2 当前网络规划

| 设备 | 地址 | 端口 | 验证结果 |
|---|---|---:|---|
| TOMILO 实验舱 | `192.168.1.30` | `8000` | Ping 与 Modbus TCP 端口正常 |
| FMC4030 控制器 1 | `192.168.1.31` | `8088` | Ping、TCP、SDK 正常 |
| FMC4030 控制器 2 | `192.168.1.32` | `8088` | Ping、TCP、SDK 正常 |
| FMC4030 控制器 3 | `192.168.1.33` | `8088` | Ping、TCP、SDK 正常 |

三台控制器 SDK 只读验证均为：

```text
Open=0
Get_Machine_Status=0
Close=0
```

## 3. FMC4030 SDK 能力确认

仓库中的头文件和示例表明 SDK 支持：

- 打开、关闭设备；
- 单轴相对/绝对运动；
- 单轴停止、运行状态检查；
- 读取位置、速度、正负限位、回零状态；
- 单轴回零；
- 两轴/三轴直线插补、两轴圆弧插补；
- 暂停、恢复、全局停止；
- 读取和设置设备参数；
- IO 输入输出；
- RS-485 数据收发；
- 下载、启动、停止和删除控制器内部脚本。

当前 C# 示例只封装了少量接口，后续需要自行补齐 P/Invoke。三台控制器相互独立，SDK 的三轴插补只适用于同一台控制器内部，不能直接完成跨控制器硬件同步。

## 4. 单轴测试脚本

已新增 [Test-FmcAxis.ps1](D:/Thermo-Vision-Lab/Test-FmcAxis.ps1)，支持输入：

```text
控制器 IP
轴号 0/1/2
相对或绝对位移
速度
加速度
减速度
未回零显式授权
单轴停止
```

脚本包含以下保护：

- 运动前读取运行和限位状态；
- 正限位触发时禁止继续正向运动；
- 负限位触发时禁止继续负向运动；
- 必须人工输入 `RUN` 才发送运动指令；
- 单次运动限制为不超过 50 个控制器单位；
- 位置超过 2 秒不变化但仍报告速度时立即停止；
- 检测到硬件限位时立即停止；
- 运动超时时立即停止。

### 4.1 脚本问题与修复

首次运行时，32 位 Windows PowerShell 将无 BOM UTF-8 文件中的中文 DLL 路径解析为乱码，导致找不到 DLL。已改为从项目目录自动搜索长度为 `23552` 字节的 C# 版 32 位 `FMC4030-Dll.dll`，不再依赖脚本中的中文路径。

SDK DLL 必须由 32 位 PowerShell 加载：

```text
C:\Windows\SysWOW64\WindowsPowerShell\v1.0\powershell.exe
```

## 5. 31 号控制器 X 轴状态与限位测试

### 5.1 初始状态

最初读取到三个通道均为：

```text
位置：0
速度：0
状态：0x0E08
```

`0x0E08` 表示轴停止、正负限位未触发、尚未完成回零。

### 5.2 机械停止但控制器仍运行

一次向限位方向运动时，现场机械轴已经不再移动，但控制器仍报告：

```text
位置：-148.17 左右
速度：2
状态：0x0E01
```

其中 `0x0001` 表示控制器仍认为轴在运行。发送 `FMC4030_Stop_Single_Axis` 后返回 `0`，复核结果为：

```text
位置：-148.414
速度：0
状态：0x0E08
```

结论：控制器位置很可能是输出脉冲/指令位置，不是编码器反馈的真实机械位置。机械堵转、顶住或电机丢步时，`position` 仍可能变化，造成显示坐标与实际位置分离。

### 5.3 正限位确认

之后读取到：

```text
X 轴位置：-109.238
速度：0
状态：0x0A28
正限位：触发
负限位：未触发
```

`0x0A28` 表示轴停止、正限位触发、负限位未触发、尚未回零。

断电重启后读取结果为：

```text
位置：0
速度：0
状态：0x0A28
```

这证明断电后显示的 `0` 只是控制器坐标初始化，不能证明机械零点已经建立。

## 6. 回零尝试与结论

### 6.1 正方向回零尝试

按“正限位作为零点”的设想，先从正限位向负方向退开 10 个单位：

```text
退开前：位置 0，状态 0x0A28
退开后：位置 -10，速度 0，状态 0x0E08
```

正限位已成功解除。随后调用正方向回零，最终状态为：

```text
位置：0
速度：0
状态：0x1228
正限位：触发
Home Done：否
Home Timeout：是
```

结论：当前正限位虽然能够被控制器识别为限位，但没有被识别为回零原点。不能继续使用普通正方向回零，否则会在限位处继续输出脉冲并等待到回零超时。

### 6.2 当前位置回零尝试

根据 Java 示例，`homeDir=3` 表示“当前位置回零”。调用结果：

```text
接口返回：0
位置：0
速度：0
Home Done：否
Home Timeout：是
```

接口虽然接受了命令，但没有建立 `Home Done` 状态，也没有清除回零超时。因此目前不能依赖 `homeDir=3` 建立控制器认可的原点。

### 6.3 回零结论

- 当前没有确认可用的独立原点输入；
- 正限位不能直接代替 SDK 的回零输入；
- 当前 DLL/固件下，“当前位置回零”没有产生可验证的回零完成状态；
- 短期采用上位机软件零点；
- 长期应安装独立原点传感器，或向厂商确认如何将正限位映射为回零输入。

## 7. 控制器参数

对 `192.168.1.31` 读取到三个通道参数一致：

| 参数 | 数值 |
|---|---:|
| 细分 `div` | `5000` |
| 导程 `lead` | `10` |
| 软件正限位参数 | `3000` |
| 软件负限位参数 | `200` |
| 回零时间 | `10000` |

若导程单位为毫米，则理论脉冲分辨率约为：

```text
10 / 5000 = 0.002 mm/脉冲
```

单位仍需通过现场尺量或百分表进一步确认。

## 8. 低速往返测试

使用以下参数进行不接触限位的往返测试：

```text
Speed = 0.5
Acc = 2
Dec = 2
先相对 +10，再相对 -10
```

控制器内部读数：

```text
起点：56.272
正向 10 后：66.272
反向 10 后：56.272
内部坐标误差：0
```

全程未触发限位，也没有检测到位置停滞。说明低速、无堵转条件下，控制器内部脉冲计数能够正常往返。

用户此前报告往返后可相差约 50 个单位。结合“机械不动但位置仍变化”的现象，主要怀疑此前轴被机械阻挡后控制器仍输出脉冲，产生约 50 个单位的虚假位置累计。软件坐标无法修复真实丢步或机械打滑。

## 9. 正限位触发与重复性测试

### 9.1 单次靠近正限位

从位置 `56.272` 低速向正方向运动，结果：

```text
起始位置：56.272
正限位触发位置：90.484
移动量：34.212
最终速度：0
状态：0x0A28
停止命令返回：0
```

本次限位正常触发，未出现提前堵转。

### 9.2 一次完整重复性测试

执行顺序：

```text
正限位 L1
→ 负方向退开 10，记录 A1
→ 正方向重新触发限位，记录 L2
→ 再负方向退开 10，记录 A2
```

结果：

| 测量点 | 控制器位置 |
|---|---:|
| 第一次正限位 L1 | `90.484` |
| 第一次退开 10 后 A1 | `80.484` |
| 第二次正限位 L2 | `90.484` |
| 第二次退开 10 后 A2 | `80.484` |

计算结果：

```text
正限位位置差 L2-L1：0
退开位置差 A2-A1：0
```

当前最终状态：

```text
X 轴位置：80.484
速度：0
正限位：未触发
```

该结果只能证明一次循环中的控制器内部脉冲坐标一致。真实机械重复精度仍需连续测试至少 10 次，并使用卡尺、百分表或固定机械标记进行独立测量。

## 10. 软件零点方案

当前建议把正限位作为上位机软件参考点，而不是依赖控制器 `Home Done`。

每次上电后的建立流程：

```text
低速靠近正限位
→ 正限位刚触发时立即停止
→ 记录当次原始位置 P0
→ 负方向退开安全距离
→ 软件 X = P0 - 控制器原始位置
```

这样定义后：

```text
正限位位置：软件 X = 0
离开正限位进入工作区：软件 X > 0
```

不能把本次的 `90.484` 写死为永久零点。断电后控制器原始坐标会重新初始化，每次上电必须重新触发限位并记录当次 `P0`。

软件零点适合当前项目，但必须同时满足：

- 限位触发后立即停止；
- 禁止继续向限位方向运动；
- 允许反方向安全退出；
- 建立软件行程范围；
- 检测堵转、超时和通信断开；
- 对真实重复精度进行外部测量；
- 软件零点不能补偿电机丢步、联轴器打滑或机械间隙。

## 11. 离线开发可行性

目前已经具备轴控制软件的离线开发条件。离开实验室后可以开发：

- 三台控制器连接、断线重连和状态管理；
- 位置、速度、限位状态轮询；
- 单轴相对/绝对运动；
- 单轴停止和全部停止；
- 软件零点与软件坐标换算；
- 软件行程限制；
- 限位方向互锁；
- 运动超时和位置停滞检测；
- 手动控制界面；
- 扫描路径生成；
- 多控制器任务协调；
- 模拟控制器服务；
- 运动日志和故障记录。

仍需回实验室确认：

- `.31/.32/.33` 与实际平台的对应关系；
- 各控制器通道 `0/1/2` 与机械轴的对应关系；
- 每个轴的正负方向；
- 负限位和完整机械行程；
- 控制器单位与毫米的实际换算；
- 安全速度、加速度和减速度；
- `.32/.33` 的实际运动；
- 多次限位重复精度和真实机械往返误差；
- 急停和断电安全策略。

## 12. 建议的软件架构

当前相机 WPF 工程为 x64，而 FMC4030 Windows DLL 为 32 位。建议将轴控制做成独立 x86 进程：

```text
x64 主程序 / 红外相机模块
        ↓ 本机 IPC 或 TCP
x86 运动控制服务
        ↓
FMC4030 32 位 DLL
        ↓
三台 FMC4030 控制器
```

运动控制服务应同时提供真实实现和模拟实现。无设备开发时使用模拟实现，回实验室后切换到真实 FMC4030 服务。

## 13. 本项目轴控制功能范围

第一版优先完成：

1. 三控制器连接与状态读取；
2. 单轴手动运动与停止；
3. 软件零点；
4. 软件限位与方向互锁；
5. 堵转、超时、通信异常保护；
6. 移动到指定软件坐标；
7. 等待所有相关轴停止；
8. 自动扫描路径；
9. “移动 → 停稳 → 红外采集 → 读取实验舱 → 保存坐标和时间戳”；
10. 暂停、继续、取消和全局停止。

暂不优先开发圆弧插补、控制器内部脚本、固件下载、RS-485 透传和普通 IO 输出。

## 14. 安全结论

- 当前不要继续调用正方向普通回零；
- 不把控制器 `position` 单独作为真实机械位置依据；
- 轴触碰机械阻挡后必须立即停止，不能等待内部位置达到目标；
- 正限位触发时只允许向负方向退出；
- 程序启动时不得自动运动；
- 自动扫描前必须完成各轴方向、行程和软件限位标定；
- 无编码器闭环时，任何堵转或丢步都可能使内部坐标失真；
- 真实精度结论必须通过外部量具验证。
