# TOMILO 实验舱 LAN 联调日志

首次记录：2026-07-16 15:09:20  
最后更新：2026-07-16 16:11:47  
设备：TOMILO 高低温湿热箱 `TOH-408EXL`  
设备编号：`T253962`

## 1. 本次目标

确认电脑通过实验舱 LAN 口能否与实验舱 HMI 建立网络通信，并初步识别可用于上位机接入的服务。

## 2. 网络拓扑与地址

本次为电脑与实验舱 LAN 口直接连接，未经过 FMC4030 控制器交换机。

| 对象 | 地址/标识 | 状态 |
|---|---|---|
| 电脑物理以太网 | `192.168.1.1/24` | 已连接，100 Mbps |
| 实验舱 HMI | `192.168.1.30` | 已确认在线，可读取数据 |
| 实验舱 HMI MAC | `0C-75-12-B7-F2-11` | 已通过 ARP 解析 |

说明：联调初期使用过 `192.168.0.1/24` 与 `192.168.0.30`；当前实验舱已切换为 `192.168.1.30`，电脑为 `192.168.1.1`。后续若切回 FMC4030 网络，须先确认网段，避免地址规划混用。

## 3. 连通性验证结果

在当前电脑地址为 `192.168.1.1/24`、实验舱地址为 `192.168.1.30` 时完成验证：

```text
Ping：成功
TCP：8000 端口可建立连接
```

结论：实验舱 LAN 物理链路、IP 配置和基础网络通信均正常。

## 4. HMI 与软件标识

根据 MAC 前缀 `0C:75:12`，实验舱 LAN 设备识别为深圳昆仑通态（MCGS）系列 HMI。

HMI 显示界面读取到的信息：

| 项目 | 值 |
|---|---|
| 设备编号 | `T253962` |
| HMI 工程版本 | `X1M7072GITH002.01` |
| HMI 工程日期 | `2025-10-09` |
| PLC 版本/日期显示 | `2026-03-11 1` |

该版本号未检索到公开的 TOMILO 通讯手册或寄存器表，属于本设备定制工程标识。

## 5. 端口检查结果

### 5.1 初始本地模式

下列常见端口未发现开放服务：

```text
80, 443, 502, 4840, 8080, 8088
```

因此，实验舱没有直接暴露常见 Web、Modbus TCP 或 OPC UA 服务。

### 5.2 切换模式后

现场切换 HMI 的模式后，再次进行仅 TCP 握手的只读检查，发现：

```text
TCP 22   ：开放
TCP 8000 ：开放
```

说明切换模式已经启用远程访问相关服务。

后续从售后取得协议文件，已确认 `8000` 是本设备的 Modbus TCP Server 端口，详见第 6 节。

## 6. 已获取通讯协议与只读验证

售后提供的协议文件：[TOMILO-L控制器通讯协议(标准版) V2.1.pdf](/D:/Thermo-Vision-Lab/TOMILO-L控制器通讯协议(标准版)%20V2.1.pdf)。

协议明确规定：

| 项目 | 参数 |
|---|---|
| 协议 | Modbus TCP Server |
| 实验舱 IP | `192.168.1.30` |
| TCP 端口 | `8000` |
| Modbus Unit ID | `1` |
| 当前温度 | 地址 `7991`，读取值除以 `10`，单位 `℃` |
| 当前湿度 | 地址 `7992` |
| 运行状态 | 地址 `7990`，`0` 为停止、`1` 为运行 |

已按厂商网络示例发送只读 Modbus TCP 报文，未包含任何写入命令：

```text
请求：00 01 00 00 00 06 01 03 1F 37 00 01
含义：站号 1，功能码 03，读取地址 7991，数量 1

响应：00 01 00 00 00 05 01 03 02 00 00
结果：地址 7991 原始值为 0，当前温度为 0.0 ℃
```

结论：实验舱 LAN 上的 Modbus TCP 通讯已实际验证成功，已经具备开发第一版只读上位机采集功能的全部必要信息。

注意：协议的“工作信息”表标题写功能码 `04`，但厂商 TCP 报文示例明确用功能码 `03` 读取地址 `7991`，且现场实测 `03` 已成功。第一版采集应以厂商示例及现场实测的功能码 `03` 为准。

## 7. 安全边界

本次仅执行了以下只读网络动作：

1. 网卡状态与 IP 配置读取；
2. ARP、Ping 连通性验证；
3. TCP 端口握手检查。

未执行以下操作：

- 未向实验舱发送温湿度设定、启停、程序运行或停止命令；
- 仅读取了 Modbus 地址 `7991`（当前温度）；
- 未尝试 SSH 登录、默认账户或密码猜测；
- 未修改 HMI 工程、PLC 程序或系统文件。

注意：HMI 画面中的“远程 IP”字段在 MCGS 类工程中可能用于 HMI 到 PLC 的连接配置。后续不要在未确认字段含义前随意改写，以免造成 HMI 与 PLC 通信中断。

## 8. 当前结论

实验舱可以通过 LAN 口与电脑通信，当前可稳定访问地址：

```text
192.168.1.30:8000
```

该端口使用 Modbus TCP，已确认可以读取温度；温度、湿度、运行状态、报警与程序状态的地址表均已获得。实验舱上位机的只读采集不再受协议、端口或 SSH 账号阻塞。

## 9. 后续建议

优先开发只读数据采集：

```text
连接实验舱
→ 周期读取温湿度、运行状态和报警
→ 保存时间戳与三维平台坐标
→ 验证数据一致性
→ 再评估是否开放远程控制
```
