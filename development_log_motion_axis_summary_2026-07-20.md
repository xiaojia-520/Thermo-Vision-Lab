# FMC4030 运动轴联调临时总结

日期：2026-07-20

## 一、当天完成的工作

### 1. 三台控制器 IP 调整

三台 FMC4030 控制器已从 `192.168.0.x` 网段调整到 `192.168.1.x`：

| 原 IP | 新 IP | 结果 |
|---|---|---|
| 192.168.0.31 | 192.168.1.31 | Open/Get/Set/Close 均成功 |
| 192.168.0.32 | 192.168.1.32 | Open/Get/Set/Close 均成功 |
| 192.168.0.33 | 192.168.1.33 | Open/Get/Set/Close 均成功 |

当前网络：

```text
电脑：192.168.1.1
实验舱：192.168.1.30:8000
控制器：192.168.1.31/.32/.33:8088
```

### 2. FMC4030 SDK 能力确认

SDK 支持：

- 打开和关闭控制器；
- 读取位置、速度和轴状态；
- 读取正负限位；
- 单轴相对运动；
- 单轴绝对运动；
- 单轴停止；
- 回零；
- 两轴、三轴插补；
- 全局停止；
- 设备参数读取和修改；
- IO、RS-485 和控制器内部脚本。

当前 C# 示例只封装了少量接口，后续需要自行补充 P/Invoke。

### 3. 单轴安全测试脚本

新增：

```text
Test-FmcAxis.ps1
```

脚本支持：

- 指定控制器 IP；
- 指定轴号；
- 相对或绝对运动；
- 指定位移；
- 指定速度、加速度和减速度；
- 单轴停止；
- 未回零授权。

脚本安全措施：

- 运动前读取控制器状态；
- 正负限位互锁；
- 必须输入 `RUN` 才发送运动命令；
- 单次运动不超过 50 个控制器单位；
- 位置长时间不变化时停止；
- 硬件限位触发时停止；
- 运动超时时停止；
- 支持 32 位 PowerShell 调用 32 位 DLL。

### 4. `.31` 控制器 X 轴测试

测试对象：

```text
控制器：192.168.1.31
轴号：0
逻辑轴：X 轴
```

完成了：

- 状态读取；
- 停止状态检查；
- 正限位测试；
- 单轴停止；
- 回零尝试；
- 低速正反往返；
- 限位重复性测试。

低速往返参数：

```text
Speed = 0.5
Acc = 2
Dec = 2
先相对 +10，再相对 -10
```

结果：

```text
控制器内部位置能够正常往返
没有触发限位
控制器内部坐标误差为 0
```

正限位重复测试：

```text
第一次正限位：90.484
第二次正限位：90.484
位置差：0
```

## 二、遇到的问题

### 1. 机械停止后控制器仍然报告运动

测试中出现：

```text
机械轴已经不再移动
控制器仍然报告速度不为零
控制器位置仍继续变化
```

发送停止命令后，控制器才报告停止。

说明控制器的位置可能是：

```text
脉冲坐标 / 指令坐标
```

不一定是真实机械反馈位置。

如果发生机械堵转、丢步或联轴器打滑，控制器位置仍可能变化。

### 2. 控制器位置不能直接作为真实机械位置

此前往返后出现几十个单位误差，结合本次测试，可能原因是：

```text
机械被阻挡
→ 控制器继续输出脉冲
→ 内部坐标继续变化
→ 软件坐标与真实机械位置分离
```

真实机械位移必须通过卡尺、百分表、固定机械标记、编码器或其他外部测量方式验证。

### 3. 正限位能够触发，但无法完成回零

正限位可以被控制器识别，但执行普通回零后：

```text
Home Done：否
Home Timeout：是
```

结论：

- 正限位只是限位输入；
- 正限位不能直接代替回零原点；
- 当前不能依赖普通回零建立原点；
- 继续向限位方向回零可能导致持续输出脉冲。

### 4. 当前位置回零也不可靠

调用“当前位置回零”后：

```text
接口返回：0
Home Done：否
Home Timeout：是
```

说明接口接受了命令，但没有形成有效的回零完成状态。

### 5. 断电后的坐标 0 不是真实机械零点

断电重启后控制器显示 `位置：0`，这只是控制器坐标重新初始化，不能证明机械位于真实原点。

## 三、当前解决方案

### 1. 使用软件零点

当前短期采用软件零点：

```text
低速靠近正限位
→ 正限位刚触发时停止
→ 记录当前控制器位置 P0
→ 反方向退开安全距离
→ 建立软件坐标
```

软件坐标：

```text
软件坐标 = 当前控制器原始位置 - P0
```

每次上电都需要重新建立软件零点，不能把某次测试得到的 `90.484` 永久写死。

### 2. 增加软件安全互锁

程序需要实现：

- 正限位触发后禁止继续正向运动；
- 只允许反向退出；
- 运动超时立即停止；
- 位置长时间不变化时停止；
- 通信断开时停止；
- 超出软件行程时拒绝运动；
- 未建立软件零点时禁止自动扫描；
- 未人工使能时禁止运动。

### 3. 长期解决方向

后续需要：

- 安装或确认独立原点传感器；
- 向厂商确认限位与回零输入的映射方式；
- 重新确认回零方向和参数；
- 使用外部量具验证真实重复精度；
- 如果需要真实闭环位置，增加编码器或反馈装置。

## 四、可以离线开发的内容

无设备时仍可以开发：

- x86 MotionHost；
- 三台控制器连接管理；
- 位置、速度、状态读取；
- 限位解析；
- 单轴停止和全部停止；
- 软件零点；
- 软件限位；
- 运动超时；
- 堵转检测；
- 手动控制界面；
- 模拟控制器；
- 扫描路径；
- 日志和故障记录。

## 五、回实验室后需要确认

- `.31/.32/.33` 分别对应哪套平台；
- 每台控制器 CH1/CH2/CH3 对应哪个物理轴；
- 每个轴的实际正负方向；
- 控制器单位与毫米的换算；
- 正负机械行程；
- 负限位是否有效；
- 原点传感器是否存在；
- 真实机械重复精度；
- 急停和断电安全策略。

## 六、建议的软件开发顺序

```text
只读状态
→ 限位解析
→ 停止控制
→ 软件零点
→ 软件限位
→ 最小距离运动
→ 手动控制界面
→ 自动扫描
```

## 七、安全结论

- 不要继续调用当前不可靠的普通回零流程；
- 不要把控制器 position 直接当成真实机械位置；
- 机械堵转后必须立即停止；
- 正限位触发时只允许反向退出；
- 程序启动时不得自动运动；
- 自动扫描前必须完成方向、行程和软件限位标定；
- 软件急停不能替代物理急停和硬件限位。
