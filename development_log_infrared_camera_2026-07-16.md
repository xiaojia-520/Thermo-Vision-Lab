# Yoseen 红外相机联调与开发日志

记录时间：2026-07-16 16:15:12 +08:00  
设备：Yoseen 网络型红外热像仪  
范围：以太网连通、IRTool Pro 温度流验证、Web 管理页识别、WPF 相机采集架构初始化

## 1. 本次目标

在运动平台因限位调整工具缺失而暂缓的情况下，独立推进红外相机的网络接入、温度流验证和上位机开发准备工作。

## 2. 网络配置与连通性

相机直连电脑物理以太网，已将电脑网卡从运动控制器/实验舱使用的 `192.168.0.1/24` 切换为相机网段：

| 对象 | 地址/标识 | 验证结果 |
|---|---|---|
| 电脑物理以太网 | `192.168.1.1/24` | 已生效，链路速率 1 Gbps |
| 红外相机 | `192.168.1.201` | 连续 Ping 成功 |
| 当前相机 MAC | `00-AA-18-19-97-05` | ARP 已解析，状态 Reachable |
| HTTP 管理服务 | `http://192.168.1.201/` | TCP 80 开放，HTTP 200 |
| RTSP 服务 | `192.168.1.201:554` | TCP 554 开放 |

结论：相机供电、以太网链路、IPv4 配置和基础应用服务均正常。

注意：2026-07-06 的既有记录中 MAC 为 `00-AA-24-25-06-45`，与本次在线设备不同。后续应以 IRTool Pro 显示的设备序列号、型号、分辨率和帧率作为当前设备的正式身份记录，避免把旧设备资料混用。

## 3. IRTool Pro 温度流验证

已使用随设备提供的 IRTool Pro 成功显示红外画面，并完成基础体温响应检查：

| 检查项 | 结果 |
|---|---|
| 实时画面 | 已正常显示 |
| 手掌表面温度 | 约 `35°C` |
| 环境温度 | 约 `21–25°C` |
| 判断 | 手掌温度显著高于环境，符合室温下人体皮肤表面温度的合理范围 |

当前可判定相机的基础温度流、画面刷新和热点响应正常。

尚未完成绝对测温精度校验。后续应使用已知温度的哑光高发射率参照物（例如贴哑光胶带的恒温表面）进行比对；不应用金属、玻璃或亮面材料作为校验对象，因为其红外反射会造成明显误差。

## 4. Web 管理页面识别

设备 Web 页面入口：

```text
http://192.168.1.201/#/login?redirect=/preview/index
```

页面标题为 `YoseenH5Device`。公开页面资源显示该管理端采用典型嵌入式单页 Web 应用结构：

```text
相机内嵌 Web 服务器
  └─ Vue 单页应用（Hash 路由）
      ├─ Vue 前端资源
      ├─ Element Plus 组件库
      ├─ 登录页 #/login
      └─ 预览页 #/preview/index
```

该 Web 页面适合作为设备配置和诊断入口；本项目正式采集仍应以 Yoseen 温度流 SDK 为主，RTSP 仅作为普通视频预览或排障备用，不能替代逐像素温度数据。

本次未尝试登录、未提交账号口令、未修改网络/测温/标定/快门等设备参数。随附手册中存在可见光机芯的默认网页登录说明，但其是否适用于当前红外 H5 页面尚未验证；后续如需配置，应先确认设备型号与厂商授权。

## 5. SDK 与 WPF 开发准备

已检查随软件分发的组件：

| 文件 | 已知信息 |
|---|---|
| `YoseenSDK.dll` | 原生 DLL，程序集读取提示无 .NET 元数据 |
| `WpfSDK.dll` | .NET WPF 封装，版本 `1.4.0.0622` |
| `IRToolPro.exe` | 厂商应用，版本 `2.4.0.0626` |
| `YoseenSDK.json` | 当前仅配置 SDK 日志和编解码器字段 |

已建立独立相机 WPF 架构骨架：

- [ThermoVision.Camera.csproj](ThermoVision.Camera/ThermoVision.Camera.csproj)：`.NET Framework 4.8`、`WPF`、`x64`；
- `ViewModels`：界面状态和异步命令；
- `Services/ICameraService.cs`：相机 SDK 的唯一调用抽象；
- `Services/DemoCameraService.cs`：仅用于验证 UI 和数据保存流程，不访问真实相机；
- `Models/ThermalFrame.cs`：采集时间、中心温度、发射率和相机 IP 的基础模型；
- `Services/JsonFrameStorage.cs`：按日期保存 JSON 采集元数据；
- [README.md](ThermoVision.Camera/README.md)：工程职责和接入说明。

现阶段 WPF 骨架尚未调用真实 Yoseen SDK，因而没有向相机发送任何指令。

## 6. 建议的数据记录格式

AI 分析和后续三维温度场重建不能仅依赖伪彩 JPG。每次正式采集应至少保存：

```text
原始逐像素温度矩阵
热图预览图
采集时间戳
相机型号/序列号/分辨率/帧率
发射率、目标距离、环境温湿度、窗口修正参数
平台 XYZ 坐标（平台恢复后）
实验舱温湿度与运行状态（协议确认后）
```

推荐目录格式：

```text
Data/
  2026-07-16/
    Run_001/
      frame_000001.jpg
      frame_000001.temperature.bin
      frame_000001.json
      run.json
```

## 7. 当前结论

红外相机链路已经具备独立开发条件，可在不依赖运动平台的前提下推进相机软件：

```text
连接相机
→ 读取设备信息（只读）
→ 显示温度流
→ 保存一帧原始温度数据及元数据
→ 连续采集与稳定性测试
→ 后续再与平台坐标、实验舱状态联动
```

## 8. 下一步待办

1. 在 IRTool Pro 的设备基本信息页记录当前设备的型号、序列号、分辨率、帧率、测温范围和镜头参数；
2. 获取厂商 SDK 的 C# 调用示例或接口文档；
3. 用 `YoseenCameraService` 替代 WPF 工程中的演示服务，优先实现只读连接、设备信息读取和单帧温度数据回调；
4. 以已知温度参照物完成基础测温精度和稳定性测试；
5. 明确温度流的原始数据格式、单位、字节序和帧率，作为 AI 数据管线输入；
6. 待轴限位问题解决后，再接入“移动 → 停稳 → 采集 → 保存坐标”的联动流程。

## 9. 安全边界

- 不在未记录当前参数前修改网络、标定、发射率、快门和固件；
- Web 管理页配置操作应先确认账号权限和字段含义；
- RTSP 只适合视频预览，不应代替温度流测温；
- 实验舱观察窗会影响红外测温，后续必须评估窗口透过率和窗口温度修正；
- 正式采集前应固定相机视场、目标距离和发射率，并记录所有变更。
