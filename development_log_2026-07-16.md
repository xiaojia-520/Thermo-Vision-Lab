# Thermo-Vision-Lab 全天开发日志

日期：2026-07-16  
项目：三维红外成像测试装置

## 一、今日工作概览

今天主要围绕实验装置的三个外部设备和上位机开发基础开展联调：

1. 完成 FMC4030 多控制器网络规划、IP 修改及 SDK 只读验证；
2. 完成 Yoseen 红外相机的网络接入、温度流验证，并建立相机 WPF 采集模块骨架；
3. 完成 TOMILO 实验舱 LAN 通信验证，确认 Modbus TCP 只读采集方式；
4. 对位移平台进行了现场确认，但轴没有继续向下运动，相关限位问题暂未处理。

## 二、具体完成内容

### 1. FMC4030 多控制器网络与 IP 配置

- 确认现场共有三台 FMC4030，分别对应两套 XY 平台和一套 XYZ 平台，共最多 7 个电机通道。
- 规划并验证控制器地址：`192.168.0.32`、`192.168.0.33` 已在线，`192.168.0.31` 已完成配置但当前未接入交换机。
- 将电脑物理网卡配置为 `192.168.0.1/24`，验证在线控制器的 Ping 和 TCP `8088` 通信。
- 编写并保存 [Change-FmcIp.ps1](D:/Thermo-Vision-Lab/Change-FmcIp.ps1)，明确了使用 32 位 PowerShell 调用 32 位 FMC4030 DLL 的 IP 修改流程。
- 对 `.32`、`.33` 控制器完成 SDK 只读验证，`Open`、状态读取和 `Close` 均正常，未发送运动指令。
- 确认通道状态 `0x0E08` 表示轴已停止、正负限位未触发、尚未完成回零。

### 2. Yoseen 红外相机联调与软件准备

- 将电脑切换到相机网段 `192.168.1.1/24`，相机地址为 `192.168.1.201`。
- 验证相机 Ping、HTTP 管理页和 RTSP 服务均正常，HTTP 返回 `200`，RTSP `554` 端口开放。
- 使用 IRTool Pro 成功显示实时红外画面，并观察到手掌约 `35°C`、环境约 `21–25°C` 的合理温差响应。
- 识别相机 Web 管理页为 `YoseenH5Device` Vue 单页应用；本次未登录、未修改设备参数。
- 检查并确认厂商组件：`YoseenSDK.dll`、`WpfSDK.dll`、`IRToolPro.exe` 和 `YoseenSDK.json`。
- 建立 `ThermoVision.Camera` 独立 WPF 架构骨架，包含 MVVM、相机服务抽象、演示服务、温度帧模型和 JSON 元数据保存功能；当前仍使用 `DemoCameraService`，尚未调用真实 SDK。

### 3. TOMILO 实验舱 LAN 与 Modbus TCP

- 电脑直连实验舱 LAN 口，配置为 `192.168.1.1/24`；确认实验舱 HMI 地址为 `192.168.1.30`。
- 验证 Ping 成功，TCP `8000` 端口可连接。
- 通过设备信息和 MAC 前缀确认 HMI 属于深圳昆仑通态（MCGS）系列，记录设备编号 `T253962` 及工程版本信息。
- 获取 [TOMILO-L 控制器通讯协议 V2.1](D:/Thermo-Vision-Lab/TOMILO-L控制器通讯协议(标准版)%20V2.1.pdf)，确认实验舱使用 Modbus TCP Server，Unit ID 为 `1`。
- 按协议发送只读功能码 `03` 报文，成功读取温度寄存器 `7991`，返回原始值 `0`，即当前温度 `0.0°C`。
- 明确当前阶段可先开发温度、湿度、运行状态和报警信息的只读采集，不开放远程控制。

## 三、遇到的问题与处理方式

### 1. 位移平台轴未继续向下运动

轴今天没有继续向下做，原因是机械限位需要先调整和确认。为避免在未确认行程、限位和回零安全性的情况下继续发送运动命令，暂时停止轴向下运动测试。

限位问题当天未能解决，直接原因是现场没有内六角工具，无法对相关限位机构进行调整。因此该问题不是通过软件绕过，后续需要准备合适规格的内六角工具后再处理。

在限位未处理前，今天只完成了控制器网络、SDK 状态读取和设备信息确认，没有进行连续大距离运动。后续处理顺序应为：调整限位 → 手动确认安全行程 → 验证回零 → 小距离相对运动 → 再接入自动扫描流程。

### 2. 多台 FMC4030 默认 IP 冲突风险

多台控制器默认可能使用 `192.168.0.30`，同时接入会引起 ARP 和设备识别冲突。处理方式是每台控制器单独连接，修改 IP、断电重启，再接入交换机，并通过 Ping、TCP `8088` 和 SDK 状态读取进行复核。

### 3. 不同设备使用不同网段

FMC4030 使用 `192.168.0.x`，红外相机和实验舱当前使用 `192.168.1.x`。处理方式是根据当前联调对象切换电脑物理网卡地址，并明确记录设备地址；后续正式系统需要统一网络规划或增加独立网卡/网段，避免接线和地址混用。

### 4. 红外相机 SDK 尚未直接接入

已确认 `YoseenSDK.dll` 为原生 DLL，现有 WPF 工程还不能直接采集真实温度流。当前先用演示服务验证界面和数据保存流程，后续需取得厂商 C# 调用示例或解析 SDK 接口，再实现真实相机服务和逐像素温度数据回调。

### 5. 测温精度尚未完成标定验证

手掌温度响应只能说明相机画面和基础温度流工作正常，不能替代绝对测温精度校验。后续使用已知温度、哑光、高发射率参照物进行比对，避免直接使用金属、玻璃或亮面材料。

### 6. 实验舱协议表述存在差异

协议表格对“工作信息”的功能码标注为 `04`，但厂商报文示例和现场验证均使用功能码 `03` 并成功读取。当前以厂商示例和实测结果为准，第一版程序先采用功能码 `03`，并保留后续复核空间。

## 四、今日结论

今天已打通三类设备的基础通信路径：FMC4030 控制器可进行网络和 SDK 只读访问，Yoseen 相机可显示实时红外温度流，TOMILO 实验舱可通过 Modbus TCP 读取温度。上位机方面已具备相机 WPF 模块和后续实验舱只读采集的开发基础。

当前主要阻塞点是位移平台限位调整：轴未继续下行是出于安全考虑，限位暂时无法解决是因为现场缺少内六角工具。补齐工具并完成机械安全确认后，再进行轴运动、回零和“移动—停稳—采集—保存坐标”的联动开发。

## 五、下一步计划

1. 准备合适规格的内六角工具，调整并验证位移平台限位和回零；
2. 确认三台 FMC4030 与 XY/XYZ 平台的对应关系，并接入第三台控制器；
3. 获取 Yoseen SDK 接口资料，实现真实相机只读连接、设备信息读取和单帧温度数据回调；
4. 开发 TOMILO 实验舱温湿度、运行状态和报警的周期只读采集；
5. 完成相机、实验舱数据与平台坐标、时间戳的统一保存；
6. 在机械运动安全确认后，再开展自动扫描流程。
