# Thermo-Vision-Lab 开发日志（2026-07-24）

## 一、详细版

### 1. 今日开发目标

今天围绕设备集成控制平台完成了以下工作：

1. 处理 Git 提交中体积过大的采集文件和生成文件。
2. 梳理并完善三台 FMC4030 运动控制器的程序架构。
3. 为三台控制器分别实现回零、实时状态和基础运动控制。
4. 将原主页面内容迁移为独立的轴体控制页面，并调整为覆盖式导航。
5. 将官方红外相机软件接入主程序入口。
6. 修复轴体控制页面删除滚动容器后出现的 XAML 语法问题。
7. 根据 TOMILO Modbus 协议完成实验舱第一版只读监控。
8. 对项目进行完整编译，并执行运动控制与实验舱相关的现场检查。

---

### 2. Git 大文件与仓库整理

#### 2.1 问题

Git 提交和推送时包含了体积较大的红外采集数据、录像、日志、编译产物和安装包，导致传输内容异常增大。

这类文件大多属于：

- 红外相机采集产生的 `.stream` 文件；
- 录像 `.mp4`；
- Visual Studio 的 `bin`、`obj`、缓存和调试文件；
- 设备运行日志；
- 本地安装包。

这些文件不适合作为源代码长期保存在普通 Git 仓库中。

#### 2.2 处理

完善了 `.gitignore`，对以下内容进行忽略：

- Visual Studio 缓存；
- C#、C++ 编译中间产物；
- 运行日志；
- 录像文件；
- IRToolPro 采集生成的 `.stream` 文件；
- 大型本地安装包。

同时对已经进入 Git 跟踪范围的大文件进行了索引清理，避免后续继续提交。

#### 2.3 当前注意事项

IRToolPro 文件夹今天从原资料目录移动到了 `src` 根目录。Git 当前会暂时显示“旧路径大量删除、新路径大量新增”，后续正式提交前需要使用 `git add -A` 重新建立索引，并检查 Git 是否正确识别为目录移动，避免遗漏官方软件依赖文件。

---

### 3. 运动控制整体架构

#### 3.1 当前架构

运动控制采用“64 位主界面 + 32 位设备宿主”的进程隔离方案：

```text
ThermoVision（x64 WPF 主界面）
            │
            │ 命名管道：命令、进度、实时状态
            ▼
MotionHost（x86 常驻进程）
            │
            │ FMC4030-Dll.dll
            ▼
192.168.1.31 / .32 / .33:8088
```

各部分职责如下：

| 模块 | 职责 |
| --- | --- |
| `ThermoVision` | 显示状态、接收用户操作、展示进度和错误 |
| `MotionHostClient` | 启动并保持 MotionHost、异步发送命令、接收状态 |
| `MotionHost` | 常驻连接控制器、轮询状态、串行执行运动命令 |
| `FmcNative` | 调用 FMC4030 厂商原生 DLL |
| `MotionSettingsStore` | 保存软件零点和软件限位配置 |

#### 3.2 采用独立 MotionHost 的原因

FMC4030 厂商 DLL 是 32 位，而主界面和红外软件相关模块使用 64 位环境。如果在主界面进程中直接加载 32 位 DLL，会出现位数不兼容问题。

今天将运动控制保持在独立 x86 进程中，解决了以下问题：

- 32 位厂商 DLL 与 64 位 WPF 主程序无法共存；
- SDK 异常可能直接影响主界面稳定性；
- 状态轮询和运动命令可能阻塞 UI；
- 多个界面重复打开控制器可能产生资源冲突。

MotionHost 现在作为单实例常驻进程运行，持续读取三台控制器状态；用户发送命令时，后台异步执行，界面不会被同步调用卡住。

---

### 4. 三台控制器与回零逻辑

#### 4.1 控制器映射

根据现场命名，完成了三台控制器的配置：

| 页面名称 | 控制器 IP | 参与轴号 | 逻辑方向 |
| --- | --- | --- | --- |
| 1 号轴 | `192.168.1.31` | Axis 0、1 | X、Y |
| 2 号轴 | `192.168.1.32` | Axis 0、1 | X、Y |
| 3 号轴 | `192.168.1.33` | Axis 0、1、2 | X、Y、Z |

#### 4.2 回零流程

三台控制器分别提供独立的回零入口：

- 1 号轴回零：依次回 X、Y；
- 2 号轴回零：依次回 X、Y；
- 3 号轴回零：依次回 X、Y、Z。

当前继续采用现场已确认的软件零点逻辑：

```text
低速寻找正限位
→ 检测到正限位后停止
→ 记录该位置为软件零点基准
→ 反方向退出 30 个控制器单位
→ 建立可用软件坐标
```

回零过程中会持续反馈当前控制器、当前方向和执行进度，并在异常时尝试停止运动。

#### 4.3 回零错误码 2

1 号控制器测试时出现“回零失败，错误码 2”。

检查发现回零程序并不是无限向正限位运动，而是设置了最大正限位搜索距离。原搜索范围不足时，轴在到达正限位前就会被软件终止，因此返回失败。

今天已将最大搜索距离调整为：

```text
MaximumSeekDistance = 150
```

正限位触发后的退出距离仍保持：

```text
ReleaseDistance = 30
```

代码和编译已经完成，但调整后的 150 行程仍需要再次进行真机回零验证。在完成验证前，不能把该问题标记为现场完全解决。

---

### 5. 轴体控制页面与基础功能

#### 5.1 页面重构

将原来放在主窗口中的轴体操作内容迁移到了独立的 `AxisControl` 用户控件中。

主窗口现在作为设备总览，轴体控制作为覆盖式子页面显示：

```text
设备总览
  └─ 点击“轴体控制”
       └─ 当前窗口覆盖显示 AxisControl
            └─ 点击“返回”恢复设备总览
```

这样不会为轴体控制重复打开新窗口，也避免把所有设备功能堆在同一个 XAML 文件中。后续实验舱、红外相机和其他设备可以按同样方式继续扩展。

#### 5.2 三台设备侧边栏

轴体控制页面左侧增加了三台控制器侧边栏，可以选择：

- 1 号轴；
- 2 号轴；
- 3 号轴。

右侧大区域根据当前选择切换显示对应控制器的：

- IP 和连接状态；
- X、Y、Z 轴状态；
- 原始位置和软件位置；
- 当前速度；
- 运行或停止状态；
- 正限位和负限位；
- 报警状态；
- 回零状态和回零超时状态。

右上角的“当前轴回零”按钮会自动对应当前选中的控制器，不再需要三个重复的大页面。

#### 5.3 今天落地的五项轴体操作

在实时状态和回零基础上，加入了以下基础控制能力：

1. **全部停止**：向三台控制器发送停止命令，并再次读取状态确认是否真正停止。
2. **步进移动**：选择 X、Y 或 Z 后，按输入距离执行正向或负向相对运动。
3. **目标定位**：输入软件坐标后，移动到指定目标位置。
4. **软件限位**：为每个控制器的每个轴保存最小、最大软件坐标范围。
5. **状态与安全联锁**：根据连接、停止、回零和软件限位状态，动态决定移动按钮是否可用。

#### 5.4 安全处理

当前程序加入了以下软件安全限制：

- 未连接时禁止运动；
- 未确认停止时禁止发送新移动命令；
- 未完成回零时禁止步进和定位；
- 未保存软件限位时禁止步进和定位；
- 目标位置超出软件限位时拒绝执行；
- 正负硬件限位会阻止继续向危险方向运动；
- 运动命令串行执行，避免同一控制器同时收到多个动作；
- 运动超时后请求停止；
- “全部停止”后读取停止状态进行确认；
- 回零或运动过程中限制离开页面；
- 执行真实运动前显示现场安全确认提示。

软件停止和软件联锁用于降低操作风险，但不能替代现场物理急停、硬件限位和人员安全确认。

---

### 6. 主页面调整

重新设计了 ThermoVision 主页面，使其作为设备总览入口，当前包括：

- 实验舱监控；
- 轴体控制；
- 红外相机。

三个模块通过统一的卡片入口进入对应功能。轴体控制和实验舱监控采用当前窗口覆盖式页面，红外相机继续启动官方独立窗口。

今天还修复了手动删除 `ScrollViewer` 后产生的 XAML 标签不配对问题。问题原因不是滚动条本身，而是只删除了容器的一部分，导致后续标签层级错乱。重新整理 Grid 层级和闭合标签后恢复编译。

---

### 7. 官方红外软件接入

#### 7.1 实现方式

根据当前阶段要求，不把 IRToolPro 界面嵌入 ThermoVision，而是在主页面点击“打开红外工具”后启动官方独立窗口。

主程序现在会：

1. 检查 `IRToolPro` 是否已经运行；
2. 如果已经运行，则恢复并激活已有窗口；
3. 如果没有运行，则查找并启动 `IRToolPro.exe`；
4. 将官方软件目录设为工作目录，保证其 DLL 和配置可以正常加载；
5. 找不到程序或启动失败时显示明确提示。

#### 7.2 路径适配

IRToolPro 文件夹移动到 `src/IRToolPro_v2.4.0.0626` 后，主程序的查找逻辑已同步调整。程序会从自身输出目录逐级向上查找该文件夹，不依赖固定的开发电脑绝对路径。

#### 7.3 遇到的问题

IRToolPro 或 ThermoVision 运行时，对应 EXE、DLL 可能被进程占用，导致正式输出目录无法覆盖。

处理方式是先关闭运行中的程序，再执行正式重建。关闭后项目已成功编译。

---

### 8. 实验舱 Modbus 只读监控

#### 8.1 第一阶段范围

实验舱提供 TOMILO Modbus 协议。为了先确认网络、地址和数据解释，今天只实现状态读取，不实现任何设备控制。

设备参数为：

```text
IP：192.168.1.30
TCP 端口：8000
Unit ID：1
```

#### 8.2 软件结构

新增 `BoxHost` 类库，结构如下：

```text
ChamberControl.xaml
        │
        ▼
TomiloChamberService
        │
        ▼
ModbusTcpClient
        │
        ▼
192.168.1.30:8000
```

各部分职责：

| 模块 | 职责 |
| --- | --- |
| `ChamberControl` | 显示温湿度、运行阶段、部件状态和报警 |
| `TomiloChamberService` | 每秒轮询、解释寄存器、断线重连 |
| `ModbusTcpClient` | 组装和校验 Modbus TCP 报文 |
| `ChamberSnapshot` | 保存一次完整的实验舱状态 |

#### 8.3 当前读取内容

当前只开放以下三种 Modbus 读取功能：

- FC02：读取离散输入。
- FC03：读取保持寄存器中的部件位状态；
- FC04：读取输入寄存器中的工作信息。

读取内容包括：

- 运行或停止状态；
- 当前温度；
- 当前湿度；
- 温度设定值；
- 湿度设定值；
- 压缩机状态；
- 温度控制和湿度控制状态；
- 升温、恒温、降温阶段；
- 加湿、恒湿、除湿阶段；
- 排水、照明、程序结束状态；
- 总报警、传感器报警、X1～X22 报警；
- 温湿度上下限报警。

代码中没有提供 FC05、FC06 或其他写入方法，因此当前版本不能启动、停止或修改实验舱设定值。

#### 8.4 运行方式

- ThermoVision 主程序启动时自动启动监控；
- 每秒读取一次设备状态；
- 返回主页面后继续保持监控；
- 通讯异常后每 2 秒自动重连；
- 关闭 ThermoVision 时释放连接。

#### 8.5 现场联调结果

已通过 `192.168.1.30:8000` 成功收到三组合法 Modbus 响应：

- FC04 读取 `7990～7993`；
- FC03 读取 `7994～7998`；
- FC04 读取 `8024～8025`；
- FC02 读取 `8055～8090`。

初次按厂商 TCP 示例使用 FC03 时，虽然通讯响应合法，但同一区域始终返回全 0。进一步对照协议寄存器表发现，`7990～8025` 实际归属 Input Registers，应使用 FC04。改用 FC04 后读到动态数据：

```text
运行状态：运行
当前温度：19.8 ℃
当前湿度：31.4 %RH
温度设定值：20.0 ℃
湿度设定值：30.0 %RH
活动报警：无
```

当前温度、当前湿度和温湿度设定值均按原始值除以 10 显示。工作信息已切换为 FC04；部件位按协议单独使用 FC03，报警使用 FC02。

现场继续验证发现，实验舱处于运行状态时，部件寄存器 `7996` 仍返回 `0x0000`。因此程序不会把压缩机、温控、湿控等错误显示为“关闭”，而是明确显示“无数据”，并保留整体运行状态和温湿度数据。整个联调和程序均未加入任何 Modbus 写命令。

---

### 9. 编译与生成结果

今天完成了 Debug 配置下的正式重建：

```text
BoxHost      -> x64 Debug 生成成功
MotionHost   -> x86 Debug 生成成功
ThermoVision -> x64 Debug 生成成功
```

主要产物：

```text
src/BoxHost/bin/x64/Debug/BoxHost.dll
src/MotionHost/bin/x86/Debug/MotionHost.exe
src/ThermoVision/bin/x64/Debug/ThermoVision.exe
```

XAML、C# 和项目引用均已通过 MSBuild 检查。

---

### 10. 今日问题与解决情况汇总

| 问题 | 原因 | 解决方式 | 当前状态 |
| --- | --- | --- | --- |
| Git 推送内容过大 | 采集数据、录像、日志和生成文件进入仓库 | 完善 `.gitignore` 并清理跟踪索引 | 基本解决，IRToolPro 移动状态需提交前复查 |
| 64 位主界面无法直接加载 FMC4030 DLL | 厂商 DLL 为 32 位 | 使用独立 x86 MotionHost，通过命名管道与 x64 主界面通信 | 已解决 |
| 三台控制器轴数不同 | `.31/.32` 为 XY，`.33` 为 XYZ | 按控制器配置轴列表和独立回零流程 | 已完成 |
| 1 号轴回零返回错误码 2 | 软件设置的正限位最大搜索距离不足 | 最大搜索距离改为 150，退出距离保持 30 | 代码已完成，待真机复测 |
| 状态读取与运动命令可能阻塞界面 | 厂商调用和运动过程耗时较长 | MotionHost 常驻轮询，命令异步执行并反馈进度 | 已解决 |
| 轴体控制打开新窗口不符合预期 | 原导航方式未统一 | 改为主窗口内覆盖显示并提供返回按钮 | 已解决 |
| 所有设备内容放在一个 XAML 难以维护 | 页面职责混杂 | 拆分 `AxisControl`、`ChamberControl` 和状态控件 | 已解决 |
| 删除滚动容器后 XAML 报错 | 标签只删除了一部分，结构未闭合 | 重整 Grid 层级和闭合标签 | 已解决 |
| IRToolPro 移动后找不到程序 | 原路径发生变化 | 改为从程序目录逐级查找 `src` 下的官方软件 | 已解决 |
| 程序运行时无法覆盖生成文件 | EXE 或 DLL 被运行进程占用 | 关闭相关进程后重新生成 | 已解决 |
| Modbus 协议接入初期存在写入风险 | 尚未充分验证现场设备行为 | 第一版只实现 FC02/FC03/FC04 读取，没有任何写命令 | 已解决 |
| 实验舱读取值全部为 0 | 厂商 TCP 示例使用 FC03，但工作信息实际属于 FC04 Input Registers | 对 FC03/FC04 进行只读对照，程序改用 FC04 后成功读到动态数据 | 已解决 |
| 部件状态无法判断 | 设备运行时 `7996` 仍返回全 0 | 按协议使用 FC03 单独读取，并将矛盾的全 0 状态标记为“无数据” | 软件处理已完成，设备映射待确认 |

---

### 11. 当前系统阶段结论

今天已经从单一运动测试程序推进到一个初步可用的多设备控制平台：

```text
ThermoVision 主界面
├─ 实验舱：已接入只读 Modbus 实时监控
├─ 轴体控制：已接入三台控制器的状态、回零和基础运动
└─ 红外相机：已接入官方 IRToolPro 独立启动入口
```

当前最完整的部分是软件架构、页面导航、运动状态链路和基础安全联锁。下一阶段仍需要重点完成真机参数标定和连续运行验证。

---

### 12. 下一步建议

1. 使用搜索距离 150 重新测试 1 号控制器 X、Y 回零，记录正限位位置和完整返回信息。
2. 分别验证 `.31`、`.32`、`.33` 的物理方向、轴号、实际行程和控制器单位与毫米的换算。
3. 为每个物理轴标定软件限位，并验证程序重启后的配置恢复。
4. 对相对运动、目标定位、正负限位阻断和全部停止逐项进行低速真机测试。
5. 对照实验舱 HMI 检查温湿度数据，确认当前返回 0 的设备侧原因。
6. 实验舱只读状态稳定后，再单独评审启动、停止和设定值写入的安全条件。
7. 确定后续红外相机是继续调用官方软件，还是使用 SDK 在主程序内直接采集数据。
8. 整理 IRToolPro 目录移动后的 Git 状态，提交前确认没有遗漏依赖或误提交采集数据。

---

## 二、汇报版

### 1. 今天完成了什么

今天主要完成了设备集成控制平台的第一轮整合，三个核心模块都已经有了可用入口。

#### 运动轴

- 接入三台运动控制器，分别对应两套 XY 轴和一套 XYZ 轴。
- 完成三台设备的实时状态显示，可以查看位置、速度、运行、限位、报警和回零状态。
- 完成按设备回零：1、2 号设备回 XY，3 号设备回 XYZ。
- 增加全部停止、步进移动、目标定位、软件限位和运动安全联锁。
- 运动控制后台改为常驻模式，持续读取状态，操作命令异步执行，不会卡住主界面。

#### 主界面

- 将系统整理为统一设备总览，可进入实验舱、轴体控制和红外相机。
- 轴体控制和实验舱在当前窗口内切换，可以随时返回总览，不再重复打开窗口。
- 将不同设备拆成独立页面，方便后续继续扩展。

#### 红外相机

- 已将官方 IRToolPro 接入主程序。
- 点击按钮可启动官方软件；如果软件已经运行，会直接切换到已有窗口。
- 适配了软件移动到 `src` 目录后的新路径。

#### 实验舱

- 根据 TOMILO Modbus 协议完成第一版只读监控。
- 已能连接 `192.168.1.30:8000`，读取温湿度、设定值、运行阶段、部件状态和报警。
- 当前版本明确不发送启动、停止或参数设置命令，先以只读方式验证协议和现场数据。
- 程序具备定时读取和断线自动重连能力。

#### 工程管理

- 处理了 Git 中采集数据、录像、日志和编译文件过大的问题，完善了忽略规则。
- 完成 x64 主程序、x86 运动控制程序和实验舱通讯类库的编译，项目整体生成通过。

---

### 2. 遇到了什么问题

1. 运动控制厂商 DLL 是 32 位，而主程序是 64 位，无法直接放在同一个程序中运行。
2. 三台运动控制器的轴数量不同，前两台是 XY，第三台是 XYZ，不能共用固定轴配置。
3. 1 号设备回零时出现错误码 2，程序在到达正限位前提前结束。
4. 运动过程耗时较长，如果直接在界面线程执行，会造成界面卡顿。
5. 原轴体控制打开独立窗口，页面较多后操作体验和维护性都不好。
6. 官方红外软件移动目录后，原启动路径失效；程序运行时还可能占用生成文件。
7. 实验舱最初虽然已经连通，但读取到的温湿度和设定值均为 0。
8. Git 中包含了较大的红外采集数据，导致提交和推送体积异常。

---

### 3. 问题怎么解决

1. 将运动控制单独放入 32 位后台进程，主界面通过内部通信获取状态和发送命令，从根本上解决 32/64 位兼容问题。
2. 为三台控制器建立独立配置，根据设备自动选择 XY 或 XYZ 回零流程。
3. 检查发现回零程序设置了最大搜索距离，将该距离调整到 150，正限位后的安全退出距离仍保持 30；需要下一次真机复测确认。
4. 将状态读取改为后台常驻轮询，运动命令改为异步串行执行，同时增加停止确认、限位检查和超时处理。
5. 将主界面改为设备总览加子页面覆盖切换，既不重复打开窗口，也方便后续加入其他设备。
6. 红外软件改为自动查找新目录，并支持复用已经启动的官方软件窗口；构建时先关闭占用文件的进程。
7. 对实验舱 FC03 和 FC04 进行了只读对照，确认工作信息实际需要使用 FC04；修改后已读到运行状态、19.8 ℃、31.4 %RH 和对应设定值，且仍未加入任何写命令。
8. 增加 Git 忽略规则并清理大文件跟踪，减少后续提交和推送体积。

---

### 4. 当前结果与下一步

目前主程序已经形成设备集成平台的基础框架：

- 运动轴具备实时状态、独立回零和基础运动控制；
- 红外相机可以从平台启动官方软件；
- 实验舱已经完成只读通讯和状态页面；
- 整体项目已经编译通过。

下一阶段重点不是继续增加大量按钮，而是进行现场验证和参数标定：

1. 复测三台设备回零和 150 搜索距离；
2. 标定每个轴的方向、行程、单位换算和软件限位；
3. 逐项验证停止、限位和异常处理；
4. 对照实验舱控制器确认温湿度零值原因；
5. 状态读取稳定后，再评审实验舱写入控制功能；
6. 确认红外相机下一阶段采用官方软件还是 SDK 深度集成。
