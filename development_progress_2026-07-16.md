# Thermo-Vision-Lab 开发进度记录

日期：2026-07-16  
主题：FMC4030 多控制器网络联调与 IP 配置

## 1. 项目总体目标

本项目目标是开发一套三维红外成像测试装置，将三轴/多轴位移平台、Yoseen 红外热像仪、恒温恒湿实验舱、样品台和 PC 综合控制程序集成起来。

最终系统需要支持：

- 多套 XY/XYZ 位移平台控制；
- 红外热像仪温度流或视频流采集；
- 位移坐标、图像、温度数据和时间戳绑定；
- 自动扫描、停稳、采集和数据保存；
- 后续三维温度场重构与分析。

## 2. 当前控制器结构

现场确认共有三台 FMC4030 控制器：

- 两套平台具有 XY 两个运动方向；
- 一套平台具有 XYZ 三个运动方向；
- 总计最多 7 个电机通道；
- 每台 FMC4030 内部通道号从 0 开始，对应 CH1、CH2、CH3。

当前已确认的网络规划：

| 控制器/平台 | IP | 端口 | MAC | 当前状态 |
|---|---|---:|---|---|
| 中间控制器 | `192.168.0.32` | `8088` | `E0-1A-91-13-20-3E` | 在线 |
| 新接入控制器 | `192.168.0.33` | `8088` | `E0-9A-8E-13-1B-33` | 在线 |
| 另一台控制器 | `192.168.0.31` | `8088` | `E0-1A-8A-13-13-29` | 当前未接入 |

电脑物理以太网配置：

```text
IP：192.168.0.1
子网掩码：255.255.255.0
默认网关：留空
DNS：留空
```

## 3. 网络联调结果

电脑通过交换机连接控制器，物理网卡为 1 Gbps。

当前最后一次检测结果：

```text
192.168.0.32：Ping 成功，8088/TCP 成功
192.168.0.33：Ping 成功，8088/TCP 成功
192.168.0.30：无响应
192.168.0.31：无响应
```

两个在线控制器均已通过 ARP 解析到稳定 MAC，并且不是通过虚拟网卡访问。

重要注意：FMC4030 默认地址通常是 `192.168.0.30`。多台控制器不能同时使用同一个 IP，否则会产生 ARP 和设备识别冲突。因此每接入一台新控制器，都要先单独连接、修改 IP、断电重启，再接入交换机。

## 4. IP 修改过程

FMC4030 的 Windows DLL 是 32 位版本，使用 32 位 PowerShell 调用：

```text
C:\Windows\SysWOW64\WindowsPowerShell\v1.0\powershell.exe
```

在 64 位 Windows 中，`SysWOW64` 目录中的 PowerShell 实际是 32 位 PowerShell。

修改 IP 的 SDK 流程：

```text
FMC4030_Open_Device
    ↓
FMC4030_Get_Device_Para
    ↓
读取完整设备参数
    ↓
只修改 machine_device_para.ip[15]
    ↓
FMC4030_Set_Device_Para
    ↓
FMC4030_Close_Device
    ↓
控制器断电重启
    ↓
Ping、TCP 8088、SDK 状态验证
```

设备参数结构中：

```text
offset 0  ：id
offset 4  ：bound232
offset 8  ：bound485
offset 12 ：ip[15]
offset 28 ：port
```

修改时只改变 `offset 12` 到 `offset 26` 的 15 个字节，细分、导程、软限位和回零时间等参数保持原值。

已经保存的脚本：

[Change-FmcIp.ps1](/D:/Thermo-Vision-Lab/Change-FmcIp.ps1)

脚本已经通过 PowerShell 语法解析检查，但保存后没有自动执行 IP 修改。

使用示例：

```powershell
C:\Windows\SysWOW64\WindowsPowerShell\v1.0\powershell.exe `
  -ExecutionPolicy Bypass `
  -File D:\Thermo-Vision-Lab\Change-FmcIp.ps1 `
  -OldIp 192.168.0.30 `
  -NewIp 192.168.0.33
```

## 5. 已完成的 IP 修改记录

### 5.1 中间控制器

```text
192.168.0.30 -> 192.168.0.32
MAC：E0-1A-91-13-20-3E
```

写入返回 `0`，断电重启后 `.32` Ping、8088 和 SDK 均正常。

### 5.2 另一台控制器

```text
192.168.0.30 -> 192.168.0.31
MAC：E0-1A-8A-13-13-29
```

写入返回 `0`，断电重启后 `.31` 曾验证成功；当前该设备未接入交换机。

### 5.3 新接入控制器

```text
192.168.0.30 -> 192.168.0.33
MAC：E0-9A-8E-13-1B-33
```

写入返回 `0`，断电重启后 `.33` Ping、8088 和 SDK 均正常。

## 6. SDK 只读验证结果

使用仓库中的 32 位 DLL：

```text
资料\FMC4030二次开发示例\FMC4030_Demo_CSharp(c#)\
FMC4030_Demo_CSharp\FMC4030_Demo_CSharp\FMC4030-Dll.dll
```

验证接口：

```c
FMC4030_Open_Device
FMC4030_Get_Machine_Status
FMC4030_Get_Device_Para
FMC4030_Close_Device
```

`.32` 和 `.33` 均验证得到：

```text
SDK_OPEN：0
SDK_STATUS：0
SDK_CLOSE：0
```

此前读取到的通道状态为：

```text
位置：0
速度：0
状态：0x0E08
```

`0x0E08` 表示轴停止、正负限位未触发、当前未完成回零。整个网络和 SDK 验证过程中没有发送运动命令。

## 7. `-3` 错误说明

当目标控制器地址不在线时，调用：

```powershell
[Fmc]::FMC4030_Open_Device(0, '192.168.0.32', 8088)
```

曾出现：

```text
connect error
add device failed
-3
```

随后确认该地址当时没有 Ping 和 TCP 8088 响应。FMC4030 SDK 文档没有为 `-3` 给出明确业务含义；实际排查时应优先检查 IP、网线、电源、端口和控制器是否已重启。

PowerShell 调用时必须使用变量符号 `$`：

```powershell
$id = 0
$buffer = New-Object byte[] 1024
```

不能写成：

```powershell
id
buffer
```

另外，`Add-Type` 加载过的 C# 类型不能在同一个 PowerShell 进程中重新定义。需要重新打开 PowerShell，或者使用新的类名。

## 8. 正式控制程序建议

建议技术栈：

```text
C#
WPF
.NET Framework 4.8
x86
Visual Studio
FMC4030-Dll.dll
```

原因：现有 Windows DLL 为 32 位，厂商已有 C# P/Invoke 示例；后续红外相机软件也有 WPF/.NET SDK，便于统一开发上位机。

建议软件分层：

```text
WPF 界面
  ↓
三套平台协调层
  ↓
单轴控制、停止、状态读取、限位检查
  ↓
FMC4030 C# P/Invoke 封装
  ↓
FMC4030-Dll.dll
  ↓
TCP/IP 控制器
```

每台控制器应配置：

- 名称；
- IP 和端口；
- 本地 SDK device id；
- 实际电机通道；
- 正负方向；
- 速度、加速度、减速度；
- 软件正负限位；
- 是否需要回零。

第一版开发顺序：

1. 同时连接 `.32` 和 `.33`，并加入第三台配置能力；
2. 只读取三个控制器的轴位置、速度、限位和状态；
3. 单独进行小距离相对运动测试；
4. 加入停止、通信超时和软件限位；
5. 开发手动控制界面；
6. 再开发自动扫描、停稳和红外采集联动。

如果三套平台由三台独立控制器组成，不能使用一台控制器内部的 `FMC4030_Line_3Axis` 做跨控制器硬件同步。适合先采用：

```text
移动到坐标
→ 等待停止
→ 等待机械振动稳定
→ 触发红外采集
→ 保存坐标、温度、图像和时间戳
```

## 9. 当前待办

- 确认 `.32`、`.33` 分别对应哪套 XY/XYZ 平台；
- 找到并接入第三台控制器，确认其 MAC；
- 决定第三台最终使用 `.31` 还是其他地址；
- 建立正式 C# x86 WPF 工程；
- 封装三台控制器的统一接口；
- 增加日志、超时、限位和急停处理；
- 在确认机械行程和回零安全后，才进行运动测试；
- 后续接入 Yoseen 红外热像仪并绑定 XYZ 坐标。

## 10. 安全注意事项

- 程序启动时不能自动发送运动命令；
- 所有运动必须有停止接口和超时保护；
- 不要在未确认机械行程前连续大距离运动；
- 回零超时状态必须单独处理；
- 多个屏幕或上位机同时控制同一控制器时，要明确控制权；
- 更改 IP 前必须确认目标地址没有其他设备占用；
- 修改 IP 后必须断电重启并重新验证；
- 网络验证阶段只做 Ping、TCP 和状态读取，不发送运动命令。
