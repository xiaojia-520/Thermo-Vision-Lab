# Thermo-Vision-Lab 开发日志（2026-07-21）

## 一、详细版

### 1. 今日目标

今天的主要目标是完成项目开发环境准备，理清解决方案与项目的组织方式，并为后续连接运动控制器搭建一个可以编译运行的基础工程。

本阶段优先处理以下内容：

1. 安装并确认 C#、WPF 和 .NET Framework 的开发环境。
2. 验证已有相机演示项目可以正常生成和运行。
3. 确定相机、实验舱和运动轴最终如何放在同一个系统中开发。
4. 创建正式的 WPF 主程序和运动控制器辅助程序。
5. 为 IP 地址为 `192.168.1.31` 的运动控制器编写第一版只读连接测试代码。
6. 解决 32 位厂商 SDK 与主程序平台位数不同的问题。

---

### 2. 开发环境安装与确认

今天安装了 Visual Studio 2026，并选择了“.NET 桌面开发”工作负载。这个工作负载包含本项目当前需要的主要组件：

- C# 编译和调试工具；
- WPF 桌面应用开发工具；
- .NET 与 .NET Framework 相关组件；
- Visual Studio 的解决方案、项目和生成工具。

安装完成后，已经能够在 Visual Studio 中打开项目、选择生成平台并执行编译。

当前项目继续以 Windows 桌面程序为目标，现阶段主要使用：

- WPF：制作主界面；
- C#：编写界面逻辑、设备控制和业务逻辑；
- .NET Framework 4.8：兼容现有原生设备 SDK；
- Visual Studio 2026：创建、配置、生成和调试项目。

---

### 3. 已有相机演示项目的生成验证

检查并生成了已有的 `ThermoVision.Camera` WPF 演示项目。

第一次生成时出现以下错误：

```text
MC1000: Could not find assembly 'System.Web, Version=4.0.0.0 ...'
```

#### 问题原因

项目使用了 .NET Framework 4.8，并间接依赖 `System.Web`，但项目文件中没有显式引用该程序集。WPF 的生成任务在处理引用时找不到它，因此生成失败。

#### 解决方法

在 `ThermoVision.Camera.csproj` 中补充了：

```xml
<Reference Include="System.Web" />
```

补充引用后项目能够成功生成并启动。

#### 当前结论

该项目目前属于相机功能的演示原型，使用的是 `DemoCameraService`。界面中的连接和温度数据是模拟结果，还没有接入真实相机 SDK。因此，这次验证说明的是 WPF 工程和演示逻辑能够运行，并不代表真实相机已经连接成功。

---

### 4. 明确解决方案与项目的组织方式

今天确定不需要为相机、实验舱和运动轴分别建立三个互不相关的解决方案。

最终采用“一个解决方案、多个项目”的组织方式：

```text
src/src.slnx
├─ ThermoVision        WPF 主程序
└─ MotionHost          x86 控制台辅助程序
```

后续还可以在同一个解决方案中继续增加设备层、公共协议层和测试项目。

两个现有项目的职责如下：

| 项目 | 类型 | 主要职责 |
| --- | --- | --- |
| `ThermoVision` | WPF 桌面程序 | 显示界面，组织相机、实验舱和运动轴的操作流程 |
| `MotionHost` | 控制台程序 | 以 x86 进程加载运动控制器 SDK，并负责与运动控制器通信 |

这里的“解决方案”相当于整个系统的容器，“项目”则是容器中可以独立生成的程序或代码模块。

---

### 5. 创建 WPF 主程序

在 `src` 目录下创建了正式的 WPF 项目：

```text
src/ThermoVision/ThermoVision.csproj
```

该项目目前已经具备 WPF 的基本结构：

- `App.xaml`：程序级资源和启动入口配置；
- `MainWindow.xaml`：主窗口界面；
- `MainWindow.xaml.cs`：主窗口对应的 C# 逻辑；
- `ThermoVision.csproj`：项目依赖、目标框架和生成配置。

今天对 `MainWindow.xaml` 做了简单的界面显示练习，用于理解 XAML 和 C# 的分工：

- XAML 更接近“界面层”，负责窗口、按钮、文本和布局；
- C# 不仅是“后端”，还可以处理按钮事件、界面状态、设备通信和业务逻辑。

WPF 主程序当前可以成功生成，产物为：

```text
src/ThermoVision/bin/Debug/ThermoVision.exe
```

解决方案已经为该项目配置了 x64 平台，方便后续接入相机 SDK 和实验舱通信模块。

---

### 6. 创建并调整 MotionHost

为了连接运动控制器，创建了：

```text
src/MotionHost/MotionHost.csproj
```

创建过程中曾误选 WPF 项目模板，导致 MotionHost 带有窗口程序结构。随后将它重新建立为控制台项目，因为该程序的主要职责不是显示界面，而是加载厂商 DLL、调用运动控制器 SDK，并将结果交给 WPF 主程序。

MotionHost 当前使用：

- 项目类型：控制台应用程序；
- 目标框架：.NET Framework 4.8；
- 目标平台：x86；
- 输出类型：`Exe`。

选择 x86 的原因是运动控制器提供的 `FMC4030-Dll.dll` 为 32 位原生 DLL。调用进程和原生 DLL 的位数必须一致，否则运行时通常会出现 DLL 无法加载或 `BadImageFormatException`。

---

### 7. 添加 x86 生成配置

今天在解决方案和 `MotionHost.csproj` 中增加了 x86 配置。

MotionHost 的关键配置如下：

```xml
<PropertyGroup Condition="'$(Configuration)|$(Platform)' == 'Debug|x86'">
  <PlatformTarget>x86</PlatformTarget>
  <OutputPath>bin\x86\Debug\</OutputPath>
</PropertyGroup>
```

解决方案 `src/src.slnx` 当前包含 `Any CPU`、`x64` 和 `x86` 平台，并建立了以下平台映射：

- WPF 主程序使用 x64；
- MotionHost 在需要生成时映射到 x86。

这使两个程序可以保留各自需要的平台位数，并继续放在同一个解决方案中管理。

---

### 8. 接入运动控制器 SDK 的基础调用

在 MotionHost 中添加了：

```text
src/MotionHost/FmcNative.cs
```

该文件使用 P/Invoke 声明厂商 DLL 中的基础函数，包括：

- `FMC4030_Open_Device`：连接控制器；
- `FMC4030_Close_Device`：关闭控制器连接；
- `FMC4030_Get_Machine_Status`：读取控制器状态；
- `FMC4030_Check_Axis_Is_Stop`：读取指定轴是否停止。

同时在 `Program.cs` 中编写了第一版连接测试逻辑，当前参数为：

```text
控制器 IP：192.168.1.31
控制器端口：8088
设备编号：0
测试轴号：0
```

程序当前的预期流程为：

1. 调用 SDK 打开控制器；
2. 循环读取 10 次控制器状态；
3. 从状态缓冲区中读取轴位置和速度；
4. 查询轴是否停止；
5. 每次读取之间等待 500 毫秒；
6. 无论中途是否出错，都在 `finally` 中关闭设备连接。

当前代码只调用连接和状态读取接口，没有调用点动、定位或回零等运动命令，因此属于第一阶段的只读连接测试。

---

### 9. 今日生成结果

目前解决方案中的两个项目均已生成成功。

#### MotionHost

```text
src/MotionHost/bin/x86/Debug/MotionHost.exe
src/MotionHost/bin/x86/Debug/FMC4030-Dll.dll
```

#### WPF 主程序

```text
src/ThermoVision/bin/Debug/ThermoVision.exe
```

最后一次检查确认两个可执行文件均已在 2026-07-21 生成。

需要特别说明：

> 当前已经确认的是“解决方案可以编译、x86 MotionHost 可以生成、SDK DLL 已位于输出目录”。今天尚未实际运行 MotionHost 并记录真实控制器返回值，因此还不能认定 `192.168.1.31` 已连接成功。

---

### 10. 今日遇到的问题及处理结果

| 问题 | 原因 | 今日处理结果 | 状态 |
| --- | --- | --- | --- |
| Visual Studio 最初缺少适合的桌面开发环境 | 尚未安装对应工作负载 | 安装 Visual Studio 2026，并选择“.NET 桌面开发” | 已解决 |
| 相机演示项目生成时报找不到 `System.Web` | .NET Framework 引用不完整 | 在项目文件中显式添加 `System.Web` 引用 | 已解决 |
| 不清楚解决方案与项目的区别 | 首次接触 Visual Studio 工程结构 | 确定采用一个解决方案管理多个项目 | 已明确 |
| MotionHost 最初创建成 WPF 项目 | 项目模板选择错误 | 重新建立为控制台项目 | 已解决 |
| 运动控制 SDK 与主程序位数不同 | 厂商 DLL 为 32 位，WPF 主程序计划使用 64 位 | 将 MotionHost 单独设置为 x86，并在解决方案中配置平台映射 | 已解决 |
| 旧式 .NET Framework 项目不会自动包含新增 `.cs` 文件 | 旧式项目需要在 `.csproj` 中列出编译文件 | 已将 `FmcNative.cs` 加入项目编译项 | 已解决 |
| SDK DLL 目前虽然在输出目录，但项目文件中没有正式的复制规则 | DLL 可能是手工复制到输出目录 | 下一步将 DLL 作为项目内容，并设置复制到输出目录 | 待完善 |
| 尚未确认控制器真实连接结果 | 今天只完成了代码与生成验证 | 下一步连接设备后运行只读测试并保存 SDK 返回值 | 待验证 |

---

### 11. 下一步开发计划

#### 第一步：固定 SDK DLL 的复制方式

将 `FMC4030-Dll.dll` 正式加入 MotionHost 项目，并设置为生成时自动复制到输出目录。这样执行“清理解决方案”或换电脑后，不会因为 DLL 丢失而运行失败。

预期项目配置类似：

```xml
<Content Include="FMC4030-Dll.dll">
  <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
</Content>
```

#### 第二步：确认电脑与控制器的网络连接

在运行程序前确认：

1. 电脑网卡与控制器位于同一网段；
2. 电脑网卡地址不与设备冲突；
3. 可以通过网络访问 `192.168.1.31`；
4. 防火墙没有拦截 SDK 使用的通信；
5. 控制器已上电，急停、限位和机械现场状态安全。

#### 第三步：运行只读连接测试

运行：

```text
src/MotionHost/bin/x86/Debug/MotionHost.exe
```

重点记录以下信息：

- `FMC4030_Open_Device` 的返回值；
- `FMC4030_Get_Machine_Status` 的返回值；
- 轴位置和速度是否合理；
- `FMC4030_Check_Axis_Is_Stop` 的返回值；
- `FMC4030_Close_Device` 是否正常完成；
- 是否出现 DLL 缺失、位数不一致、网络超时或访问冲突。

这一阶段继续保持只读，不发送任何运动指令。

#### 第四步：核对 SDK 文档中的状态数据定义

确认 `Get_Machine_Status` 返回缓冲区中各个字段的准确偏移、数据类型和单位，避免虽然能读到字节，但把位置、速度或状态解释错误。

#### 第五步：完成安全状态读取

在允许轴运动前，至少读取并显示：

- 控制器连接状态；
- 当前轴位置；
- 当前速度；
- 运行或停止状态；
- 正负限位；
- 报警状态；
- 急停状态；
- 伺服使能状态。

#### 第六步：再开发最小运动测试

只读测试稳定后，再根据 SDK 文档逐步加入：

1. 停止命令；
2. 伺服使能；
3. 低速、短距离的单轴点动；
4. 单轴定位；
5. 回零；
6. 多轴协调与实验流程。

每一步都应先检查报警、限位、急停和停止能力，不应直接编写大距离运动。

#### 第七步：连接 WPF 与 MotionHost

真实控制器测试稳定后，为 WPF 主程序与 MotionHost 增加进程间通信。计划使用命名管道：

```text
WPF 主程序（x64）
        │
        │ 命令与状态消息
        ▼
MotionHost（x86）
        │
        │ FMC4030 SDK
        ▼
运动控制器 192.168.1.31
```

WPF 主程序负责显示和操作，MotionHost 负责厂商 DLL 调用。即使某个 32 位 SDK 出现异常，也能尽量避免直接拖垮主界面进程。

---

## 二、汇报版

### 今日完成了什么

1. 完成 Visual Studio 2026 和“.NET 桌面开发”环境安装，C#、WPF 和 .NET Framework 项目已经可以正常生成。
2. 修复已有相机 WPF 演示项目缺少 `System.Web` 引用的问题，项目现已能够生成和启动。
3. 明确整体架构采用“一个解决方案、多个项目”，后续在同一系统中集成相机、实验舱和运动轴。
4. 创建 WPF 主程序 `ThermoVision`，用于后续承载设备状态、操作界面和实验流程。
5. 创建 x86 控制台程序 `MotionHost`，专门负责加载 32 位运动控制器 SDK。
6. 在 MotionHost 中加入 FMC4030 SDK 的连接、状态读取、停止状态检查和关闭连接接口。
7. 编写了针对 `192.168.1.31:8088` 的第一版只读连接测试程序。
8. 完成解决方案平台配置：WPF 主程序使用 x64，MotionHost 使用 x86；两个项目当前均已生成成功。

### 今日遇到了什么问题

1. 相机演示项目生成时提示找不到 `System.Web` 程序集。
2. 初期不清楚解决方案、WPF 项目和控制台项目之间的区别。
3. MotionHost 第一次误建成 WPF 项目，不符合设备 SDK 宿主程序的定位。
4. 厂商运动控制 DLL 是 32 位，而主界面计划使用 64 位，不能直接在同一个进程中加载。
5. 旧式 .NET Framework 项目不会自动编译新建的 `FmcNative.cs`，需要手动加入项目文件。
6. SDK DLL 现在位于输出目录，但还没有在项目中配置稳定的自动复制规则。
7. 当前只完成编译验证，尚未获得真实控制器的连接和状态返回结果。

### 问题如何处理、下一步如何解决

1. 已通过补充 `System.Web` 引用解决相机演示项目的生成错误。
2. 已将 MotionHost 改为 .NET Framework 4.8 控制台程序，并配置为 x86，从进程层面解决 32 位 SDK 的兼容问题。
3. 已将 WPF 与运动控制 SDK 分成两个独立程序，后续通过命名管道通信，避免位数冲突。
4. 下一步先把 `FMC4030-Dll.dll` 加入项目并设置为自动复制，保证清理和重新生成后 DLL 仍在正确目录。
5. 然后检查电脑与 `192.168.1.31` 的网络配置，运行只读连接测试，记录 SDK 各接口的实际返回值。
6. 连接成功后继续核对状态缓冲区定义，补全报警、限位、急停、伺服和停止状态读取。
7. 所有安全状态确认无误后，才进行低速、短距离、单轴的最小运动测试。
8. 运动控制验证稳定后，再把 MotionHost 的连接和状态功能接入 WPF 主界面，并继续集成相机与实验舱。

### 当前阶段结论

今天已经完成了从“安装环境和理解工程结构”到“建立 WPF 主程序、建立 x86 运动控制宿主、成功编译 SDK 调用代码”的基础工作。当前工程结构和平台配置已经基本正确，下一阶段的核心任务是真机只读连接验证。真实连接验证完成前，暂不把运动控制部分标记为已联调完成。
